spring:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    url: "jdbc:postgresql://localhost:5432/fs13"
    username: postgres
    password: pg123456
#    data: classpath:data.sql
#    initialization-mode: always